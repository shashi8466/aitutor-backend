# AIPrep365 — Application Change Log

**File:** `CHANGELOG.md`
**Purpose:** Track every application change, bug fix, UI change, feature, improvement, and pending task in one central place — instead of scattered across chats, screenshots, and task messages.

## How to use this file

- Newest entries go at the top, grouped by date (`YYYY-MM-DD`).
- Each line is one change: a short **tag**, what changed, and (for bug fixes) the root cause in one clause — not just the symptom.
- Tags: `[Bug Fix]` `[UI]` `[Feature]` `[Improvement]` `[Backend/Data]` `[Docs]`
- Reference the commit hash where one exists, so the entry links back to the actual diff.
- Keep **Pending / Known Issues** below up to date — move an item into the dated log (with the commit that resolved it) once it ships, instead of deleting the history of it having been a problem.
- This file is the source of truth for "did X ship yet, and why was it changed" — prefer updating it over re-explaining a fix in chat.

---

## Pending / Known Issues

- **Legacy DOCX-imported questions have no `question_number`.** The parser/upload pipeline now stamps every newly-imported question with its source-order number (commit `6531c21`), but the ~10,700 questions imported *before* that fix still have `question_number = NULL`. They still display and sort correctly (falling back to `id` order), just without their original Word-document number shown. No bulk backfill has been run — would need to be done deliberately if wanted, since it touches all existing rows.
- **Broader mobile/performance audit deferred.** The 2026-08-14 work (`f036160` and related) was explicitly scoped to "targeted fixes first" (Tutor dashboard, Student My Courses) rather than a full page-by-page audit across Admin/Tutor/Student/Parent. A general audit was agreed to happen later, on request.
- **Character-substitution bugs (γ/gamma, "times"→"X") are fixed at the render layer, not the data layer.** `MathRenderer`'s over-eager LaTeX-recovery heuristic (fixed in `cdf5fb7`) no longer *corrupts* text on display, but any already-saved question text that was hand-edited through the previously-broken admin editor may still contain corrupted content baked into the database. Not retroactively repaired — flagged if a specific bad question turns up.

---

## 2026-09-02

- **`e9bab83`** `[Feature]` Added Total/Active/Inactive/Needs Attention stat cards to the top of the Tutor Student Roster, reusing the same status classification already driving the filter tabs so counts are always accurate; cards double as filter buttons synced to the existing tabs.
- **`d082816`** `[Bug Fix]` **[Backend/Data]** Tutor Student Roster and the sidebar/Dashboard "Total Students" stat were built from `profiles.assigned_courses` (every student enrolled in any course the tutor is assigned to teach) instead of the students the tutor actually organized into their own Student Groups — a tutor saw far more students than they manage (82 vs. 9 unique in one reported case). Added `getTutorGroupStudentIds()` (owned-or-co-tutor groups → unique `group_members`), reusing the existing Groups-list ownership pattern; admins remain unscoped. Also added a group-membership fallback to the roster's Recent Completed Tests expand-row authorization so a visible student can never 403 there.
- **`d3c14d2`** `[Bug Fix]` A regular course's Easy/Medium/Hard attempts are three separate `test_submissions` rows, so Recent Completed Tests (Tutor Student Roster and Student Groups Analytics) showed the same topic 2–3 times with different per-level scores. `getRecentCompletedTests` now groups by `course_id` and combines each topic's latest Easy/Medium/Hard attempts into one row (reusing the same rule as `analyticsService.getTopicCombinedReport`), showing a combined score + Levels indicator when fully completed, or "Progress: X/3" otherwise. Full-Length Test submissions are untouched — still one row per attempt.
- **`9371956`** `[Bug Fix]` Test Review page question text had no dark-mode color at all (`text-gray-900` with no `dark:` variant), rendering near-black text on the dark card. Fixed that, the reading-passage panel's same gap, and brightened the Explanation section and Your Answer/Correct Answer boxes for better contrast.
- **`14def77`** `[UI]` **[Bug Fix]** Leaderboard.jsx and UniversalLeaderboard.jsx used a hardcoded `#0B0D14`, visibly different from the `gray-900` background used by the sidebar and every other Student Portal page. Changed both to `bg-gray-900` for a continuous surface.
- **`0f1d75d`** `[UI]` Student Dashboard's background and every card used the "slate" color family while every other Student Portal page resolves to "gray" via `StudentLayout`. Converted the page background and all cards to the shared `bg-white/dark:bg-gray-800` + `border-gray-100/dark:border-gray-700` pattern; the Congratulations/Target card keeps its distinct purple gradient as an intentional accent.
- **`5a390b2`** `[Bug Fix]` Course card flipped to "100% Completed" the moment a single level (e.g. just Easy) was submitted, since any completed `test_submissions` row satisfied a loose "some" check. Now requires Easy, Medium, and Hard to each be completed (matching the `REQUIRED_LEVELS` rule in `analyticsService.getTopicCombinedReport()`) — partially-done courses show "Continue Course" at 33%/67% instead.
- **`4a5495d`** `[UI]` **[Bug Fix]** Group Dashboard Top 10 empty-state cards were stretching to match the tallest card in their grid row, and long badge titles could collide with the "Max 800"/"Max 1600" text. Cards now size to their own content and the title badge wraps within its own box while Max Score stays pinned top-right — applied to all 6 cards.
- **`bba2eef`** `[UI]` **[Bug Fix]** Tutor Student Roster table is wider than the viewport on common screen sizes, so the Actions column (email/view profile) sat past the visible edge. Made it sticky to the table's right edge.
- **`88526ab`** `[Bug Fix]` "Generate a similar practice question" had no subject boundary at any layer, so a Reading & Writing question could return Math (or vice versa) when topic-name matching fell through to a broader fallback. The question's subject is now resolved from its course (`tutor_type`/`category`/`name`) and threaded through as a hard `course_id` filter at every retrieval tier, then re-validated on the selected result. `questions.section` was considered and rejected — its "general" bucket alone spans both subjects. Also fixed a blank/filler-only topic value in the question bank matching every search.
- **`3ab336b`** `[UI]` The initial no-scroll sidebar pass made the nav list feel congested; widened per-item padding/margins and trimmed more room from the logo header/footer so items breathe while the sidebar still fits without a scrollbar.
- **`874f54b`** `[UI]` Both Student and Tutor sidebars are now fixed-height and never scroll internally; item, header, and footer spacing tightened so every section/item still fits within a typical viewport with no scrollbar.
- **`b11b777`** `[Feature]` **[Bug Fix]** Easy/Medium/Hard were each independently triggering their own `TEST_COMPLETED` email to the student and every linked parent. Now a regular course's three levels fire a single combined email (reusing the existing combined report) once all three are done, deduped so a retake never re-sends it. Full-Length Test emails gained a Download button and completion time. Also fixed the new `TOPIC_COURSE_COMPLETED` event being silently blocked by the per-recipient notification-preference filter, which only recognized the old `TEST_COMPLETED` event name.
- **`0367813`** `[Feature]` Tutors can now click a student row in Students or the group Student Performance table to see their 10 most recent completed tests inline (View/Download Report) without leaving the page; Group Analytics scopes strictly to that group's assigned content server-side.

## 2026-09-01

- **`6f48fcd`** `[Bug Fix]` Quiz completion screen classified a topic's subject by sniffing keywords in question section/topic text, falling back to a name/category substring check, and if that also failed, showed BOTH Math and Reading & Writing cards — many real topic names (e.g. "Text Structure and Purpose") don't contain "read" or "math." Now classifies via `courses.tutor_type` and renders exactly one subject card. Also added a migration fixing Supabase Security Advisor's "Function Search Path Mutable" warnings across every public-schema function.
- **`95077d6`** `[Feature]` Rebuilt the Parent Portal as a sidebar shell reusing the exact Student/Tutor components and analytics (Dashboard, Test History & Review, Student Analytics drill-down) switched to a `linked_students`-authorized data path instead of a Student Group, replacing the standalone hand-rolled Parent Dashboard. Added Overview, Course Breakdown, Leaderboard, and Profile Settings pages. Also added bulk student removal to Manage Students, fixed the "already a member" filter there never actually excluding anyone, and replaced a fragile sticky+framer-motion sidebar with a plain fixed-position one in both Parent and Tutor shells.

## 2026-08-31

- **`19b23df`** `[Bug Fix]` **[UI]** Word's `<m:acc>` accent element (used for arc/hat/tilde notation like "minor arc AC") had no handler in the shared OMML-to-LaTeX converter, silently dropping the arc symbol. Also removed the two balloon decorations from the Target Score Achievement card.
- **`b77c75c`** `[Bug Fix]` Weakness Drills: removed a per-topic Free/Premium check that shouldn't gate a topic once the plan-wide Weakness Drills feature is enabled. `planService.getUsageStats` was querying a nonexistent `grading_submissions` table (silently 404ing, always returning zero usage — meaning `PracticeTests.jsx`'s "max tests per plan" limit was never enforced); fixed to query `test_submissions`. `authService.getSessionUser`'s `getSession()` call had no timeout and could hang indefinitely; raced it the same way the `getUser()` fallback already was.
- **`a011723`** `[Feature]` Extended "Download Group PDF" to every level of the Group Content Analytics hierarchy (Section/Topic/Subtopic), reusing the existing shared PDF template and export components rather than building separate logic per level.
- **`2c756af`** `[UI]` Empty-category state on My Courses now reads "Your tutor hasn't assigned any tests to your account yet. Check back soon!" instead of a generic "Coming Soon" badge.

## 2026-08-28

- **`a837700`** `[Improvement]` `getGroupDashboard` was calling `getStudentDashboard` once per group member to compute canonical scores, each redundantly re-fetching data the caller already had — for a 101-student group this meant 100+ redundant round-trips per page load. Replaced with an in-memory computation over already-fetched submissions, reproducing the same scoring rule with zero additional queries; load time dropped from ~3.9s to ~0.3s for a 101-student group, verified identical output before/after. Also replaced the blocking spinner with a skeleton layout, added a Retry button, and added stale-response protection.
- **`439d2b9`** `[Feature]` **[Bug Fix]** Added an independent Full-Length Test Top 10 leaderboard to Student Groups (separate from the existing Regular Course Top 10), so a group with only a Full-Length Test assigned now shows a populated leaderboard. Also fixed the Student Performance Table rendering 9 body cells against a 6-column header, shifting accuracy/tests/progress/study-time values 3 columns out of place.
- **`3a72d7a`** `[Bug Fix]` `getGroupDashboard` computed Top 10 leaderboards and per-student scores from `math_scaled_score`/`reading_scaled_score`, which are only meaningful for a genuine multi-section (Full-Length Test) submission — for a regular topic-level submission these columns default to a hardcoded 200, and a `||` fallback chain treated that as a real score. Removed that calculation; the group dashboard now reuses the same already-correct `getStudentDashboard` scores used by Student Analytics. Verified against live data: two students corrected from 200 to their actual 800.
- **`dff2960`** `[Bug Fix]` Regular-course submissions were persisted with the wrong `is_correct` for any question whose `correct_answer` uses the "X or Y" format, because the Postgres grading function's comparison only split on comma/pipe while the quiz UI's JS grading already treated " or " as a separator — confirmed directly against production `test_responses` rows. Extended the SQL function's split pattern to match.
- **`7cd01aa`** `[Feature]` A failed Easy/Medium/Hard practice attempt now offers "Retry {Level} Level" (fetching a fresh question set for the same course+level) alongside "Back to My Courses," instead of only the latter.

## 2026-08-27

- **`3ab1f5c`** `[Bug Fix]` Some completed regular-course attempts had a correct score summary but zero per-question detail, because the score summary and question-wise breakdown are written by the same Postgres function but read from two different tables that had drifted out of sync for a meaningful fraction of submissions. Added a migration re-asserting the correct function. Historical affected attempts aren't retroactively recoverable — the report now shows an honest "not available" message instead of "Question 1 of 0."
- **`054f685`** `[Bug Fix]` Student Group Report: Full-Length Test submissions no longer run through the Easy/Medium/Hard combining path (which could never mark them complete), so a completed FLT now shows "Completed" with its real score. Regular-course scores no longer default to 400/400/800 when nothing's been completed — they show "-" instead. Added a separate Full-Length Test Scores section, and "View Report" on a completed FLT row now opens that attempt's own report instead of the Easy/Medium/Hard combined view.
- **`bab725b`** `[UI]` Removed the gray background box behind practice quiz passage text.
- **`bb38018`** `[Bug Fix]` The shared OMML-to-LaTeX converter had no handler for Word's `<m:bar>` element (overbar/underbar notation, e.g. line-segment references like AB), silently dropping the bar on import. Added a case emitting `\overline{...}`/`\underline{...}` per the OOXML spec, shared by both client and server DOCX import paths.
- **`6824e2e`** `[UI]` Redesigned the Admin course category tabs (SAT/ACT/AP/Full Length Tests) as a segmented control instead of plain underlined text links.
- **`6771324`** `[Improvement]` A dynamically-imported chunk 404ing (tab left open across a deploy) showed a technical error screen with a raw stack trace. Added a global `vite:preloadError` listener with a bounded retry budget and reworked the ErrorBoundary to show a neutral "Loading the latest version..." state instead. Also reordered `firebase.json`'s header rules so immutable asset caching and no-cache `index.html` apply unambiguously.
- **`30ad5bb`** `[Bug Fix]` `jodit-react` destroys and rebuilds its editor instance whenever its config/editorRef props change identity; those were inline literals recreated on every render, so any unrelated form-state change tore down and rebuilt every mounted editor mid-paste. Hoisted the static config to module scope and memoized per-field props.
- **`b3065d1`** `[Feature]` `correct_answer` can now use " OR " as an alternative-answer separator (e.g. "7/2 OR 3.5"); a student may submit multiple comma/pipe/OR-separated values and is marked correct as long as every value they entered matches an accepted answer.
- **`3aaeaa2`** `[UI]` Restyled the group-invitation landing page ("You've Been Invited!") to a dark navy card with a glowing indigo border, sparkle decorations, and gradient CTA buttons.
- **`0235512`** `[Bug Fix]` **[Feature]** **[UI]** Tutor Panel's 6 pages each gated their own data fetch behind an unrelated dashboard-stats loading state, so a slow dashboard call blocked every other page — removed that gating. Student Dashboard: replaced "Continue Learning" with a Target Score Achievement banner and a Top Scores card. Fixed small mobile tap targets on the exam Check Work screens, the AI Tutor chat composer being pushed off-screen, and Calendar's hover-only edit/delete buttons on touchscreens. Added a distinct "-6 and 4" (all-required) short-answer convention alongside the existing "any one accepted" convention.

## 2026-08-26

- **`98c8d9f`** `[Bug Fix]` Native DOCX Word tables convert to raw HTML with only a faint border and no background/text color, and were never styled outside the Knowledge Base viewer. Generalized the existing table CSS rule to apply globally, fixing every already-imported question retroactively with no DB backfill needed.
- **`c3e5542`** `[Feature]` **[Bug Fix]** Added a hierarchical Group Content Analytics drill-down (Section → Topic → Subtopic) scoped to a group's actually-assigned content, backed by one shared resolver reusing the existing per-topic combined-report calculation. Fixed the Student Report's SAT Math/R&W/Overall cards averaging every individual Easy/Medium/Hard submission (including in-progress topics) instead of aggregating only fully-completed topics, and fixed the underlying subject classifier reading `courses.category` (the domain name, e.g. "Algebra") instead of `tutor_type`.
- **`cbc0805`** `[Feature]` **[Bug Fix]** Added per-group Co-Tutor access management. Fixed students getting "Access Denied" on content added to a group after they joined, by resolving access via a live group-membership check instead of a stale enrollments snapshot taken at join time. Fixed numeric free-response answers losing their negative sign during DOCX import (the "Answer:" parser regex swallowed a leading minus as punctuation). Group Student Report now shows one row per topic instead of per difficulty level.

## 2026-08-24

- **`5c1241c`** `[Docs]` Added CHANGELOG.md to track application changes going forward.
- **`ff12cbd`** `[UI]` Redesigned the student Upgrade page into two side-by-side plan cards with a feature grid and CTA banner; removed displayed pricing for now.
- **`b6eca2c`** `[Feature]` **[Bug Fix]** Students in a group now get the union of every group's assigned content plus their existing direct/plan access, enforced server-side (RLS + grading submit checks), not just hidden in the UI. Also fixed the group leaderboard silently dropping members with no matching submissions instead of showing them unranked, and false substring matches in the "Assign Content" tree (e.g. "Linear functions" matching "Nonlinear functions").

## 2026-08-21

- **`3d4a41c`** `[Feature]` Student dashboard now shows a congratulations banner once the student's overall score reaches or exceeds their configured target.
- **`0bd8cd5`** `[UI]` Report Back buttons used plain gray text with no dark-mode variant, nearly invisible on dark report backgrounds — switched to a solid blue pill.
- **`305dc6b`** `[Feature]` **[Bug Fix]** Full-Length Test "Practice Quiz" was rendering the dark fallback UI instead of the light `PracticeQuizUI` used elsewhere, due to a stray `!isAdaptive` render-branch condition. Added View/Download Report buttons to the FLT practice completion popup, and fixed the report's Back button remounting the quiz fresh instead of restoring the completion popup.
- **`666fe17`** `[UI]` Redesigned Admin Group Management to match the Tutor Panel's Student Groups UI (card layout, stat cards, tabbed Edit Group/Manage Students modal), retaining admin-only capabilities (All Tutors filter, per-card Tutor badge, Reassign-tutor action, cross-tutor stat cards).
- **`99ddcd4`** `[Bug Fix]` "View Report" from Student Groups always rendered the regular topic-quiz report even for Full-Length Test attempts. Now applies the same full-length detection check used elsewhere and renders the same `AdaptiveResultsDashboard` the student's own dashboard uses.
- **`46c5f7c`** `[Bug Fix]` Group Dashboard vs Student Report SAT Math score mismatch: score aggregation was gated behind an `isMath` category text check that Full-Length courses never match, silently dropping every real Math score and falling back to a hardcoded 400 default. Split score aggregation from question-count/accuracy aggregation so scores count whenever the section's score is present. Verified against live data for all 22 students in a real group — exact match with the Group Dashboard leaderboard.

## 2026-08-20

- **`ced7f57`** `[UI]` Removed Parent Name/Email/Mobile fields and their OTP verification flow from student sign-up — confirmed these were only ever optional passthrough metadata with no DB constraint.
- **`4261d33`** `[UI]` Moved Full-Length Tests to the first Courses category tab position and made it the default on page load.
- **`12c76ef`** `[Feature]` **[Bug Fix]** Redesigned the Group PDF report (was dumping raw overview keys, with a field-name bug rendering bare "%" for every student's average score) to match the live Group Dashboard's theme. Built a real Student PDF report — the old code only handled a different data shape than the backend actually returns, so "Download Student PDF" previously produced a blank page.
- **`934f279`** `[UI]` **[Bug Fix]** Create Student Group modal had no height cap, overflowing the viewport on shorter screens. Capped it at `max-h-[90vh]` with a fixed header/footer and internally-scrolling middle section.
- **`c93a9e0`** `[Bug Fix]` A brand-new demo lead insert discarded the database-generated id, so the first demo test completion for a given email+course produced a result email linking to an undefined report URL. Retakes were unaffected.
- **`c0849c4`** `[Bug Fix]` Demo Full-Length Test report showed modules out of attempt order because `moduleAnswers` is stored as a JSONB object (whose key order Postgres doesn't preserve) — rebuilt the report's question list from `moduleHistory` (a JSON array) instead.
- **`37e3518`** `[Bug Fix]` Result-email report/dashboard links were built from an env var that on the live deployment still resolved to the legacy Firebase Hosting domain from before the aiprep365.com migration. Added a `sanitizeAppUrl()` guard that rejects that legacy domain and falls back to the real production app.
- **`4736c6d`** `[Bug Fix]` **[UI]** Bulleted/ordered lists in the rich text editors inserted real `<ul>/<ol>` markup, but Tailwind's Preflight reset zeroed out list styling globally, rendering them invisible. Also fixed the Remove Option button (never appeared with only 2 options, never adjusted the positional `correct_answer` letter).
- **`041bfb6`** `[Feature]` Added a shared answer-grading utility (exact + numeric/fraction match) used consistently across the DOCX parser, all exam engines, and grading routes, so a Short Answer question can accept multiple written forms of the same answer.
- **`17ded78`** `[Bug Fix]` Retaking a demo test updates the same lead row, but the result-email dedup check keyed only on that row's id — so once the first completion's email sent, every later retake was permanently blocked. Keyed the dedup on lead id + completion timestamp instead. Separately, the outbox's "mark as sent" update wrote to a nonexistent column, silently failing on every send and leaving hundreds of notifications stuck in "processing" — removed the dependency on that column and added error logging.
- **`b4a5084`** `[Bug Fix]` Homepage/admin "Copy Demo Link"/lead-table report links still hardcoded the old `/demo/` path despite the route rename — pointed all of them at `/test/`/`/report/`.
- **`a02953c`** `[Bug Fix]` Concurrent 401s on an exam page each independently called `refreshSession()`, racing over the same single-use refresh token; a losing call could tear down the session and trigger a real logout mid-exam. Shared one refresh call across concurrent 401s and re-verify before honoring a `SIGNED_OUT` event.
- **`4057feb`** `[Feature]` **[Bug Fix]** Demo test report now reuses the real `AdaptiveResultsDashboard` component instead of a separate hand-built design, fixing Strengths & Weaknesses showing all zeros. Result emails now send server-side via the notification outbox the moment a test genuinely completes, instead of a client-only flow gated on the browser's PDF generation succeeding. Email verification now lets a student fix a typo'd email before OTP, and hard-enforces server-side that a demo lead's email actually passed OTP.
- **`aadb1d4`** `[Feature]` **[Bug Fix]** Redesigned Tutor Student Roster into per-student profiles with real course/test tracking — was showing one row per student-per-course-enrollment, and course-progress numbers were computed from a table that structurally can never mark a full-length test complete.

## 2026-08-19

- **`d00880d`** `[Feature]` **[Bug Fix]** Wired up the previously-orphaned group invitation link flow: opening the link now auto-adds the student to the group and enrolls them in its assigned courses, idempotently. Also fixed admin group creation writing to a nonexistent column, and tutor-created groups never getting an invite token.

## 2026-08-18

- **`a491bf6`** `[UI]` **[Bug Fix]** Leaderboard Top 5 podium redesign: dropped the fixed "podium elevation" card heights that made rank 1–3 look oversized at real 100% browser zoom; replaced faint gradient tints with clearly distinct solid gold/silver/bronze coloring so 1 > 2 > 3 reads at a glance; lightened medal badge backgrounds so the 🥇🥈🥉 emoji are actually legible. Fixed rank #4/#5 mini-cards showing `#4` twice when two students tie on score (was labeling by tied rank instead of list position). Fixed the score text (`400 / 1600`) wrapping onto a second line by giving the score `whitespace-nowrap flex-shrink-0` and letting the name truncate instead.
- **`142ff77`** `[Bug Fix]` **[UI]** **[Backend/Data]** Combined fix batch:
  - Exam question interface: `MathRenderer` now distinguishes a real paragraph break (blank line in source) from a simple line wrap, so multi-paragraph passages (e.g. Cross-Text Connections "Text 1 / Text 2") don't render as one cramped block. Removed conflicting `prose`/`whitespace-pre-wrap` classes in `PracticeQuizUI` that were fighting with `MathRenderer`'s own line-break handling.
  - Finish button: was giving zero visual feedback while the submission saved and stayed clickable, allowing accidental double-submission. Added a "Finishing…" disabled state and a re-entrancy guard in `handleFinishQuiz`.
  - Test History & Review: combined SAT topic cards and individual attempt cards (ACT/AP/Full-Length) were rendered as two separate fixed blocks (combined always first), so a newly-completed ACT/AP/Full-Length attempt could never reach the top of the page. Merged into one list sorted by actual completion timestamp. Collapsed duplicate individual-attempt cards for topics that already have a combined card. Restyled individual cards to match the combined card's design exactly (this also fixed a button-row CSS overflow bug) and dropped score-based red/orange coloring in favor of one consistent blue theme, per explicit design request.
  - **Leaderboard scoring root-cause fix:** `grading.js`'s `/universal-leaderboard` classified a submission as "SAT Math" vs "SAT Reading & Writing" purely by checking whether the course name contained the word "math" — Full-Length Test submissions (whose names never contain "math") were silently being counted as R&W attempts, contaminating SAT Overall/Math/R&W rankings. Verified against real data: Full-Length Test scaled scores (1, 7, 21, 24, 73) were being folded into a student's R&W score pool. Replaced with a reliable classifier based on `main_category`/`tutor_type`/`is_adaptive` (same convention used elsewhere in the app). Topic/Subtopic categories now match exactly on `courses.category`/`courses.name` instead of loose substrings. SAT Overall no longer fabricates a missing Math or R&W half with a default `400` — a student isn't ranked for "SAT Overall" until both halves are actually complete.
- **`d88375c`** `[Bug Fix]` Question editing was silently failing to save: `QuestionForm` submitted `formData.courseId` (camelCase) directly to Supabase, but the `questions` table column is `course_id` (snake_case) — PostgREST rejected the whole update (`PGRST204`), so **no edit-save on an existing question was ever actually persisting**, and the failure was invisible until the same-day fix below started surfacing `{error}` instead of swallowing it. Also fixed the same mismatch in `QuestionCard` (`question.courseId`/`question.uploadId`), which was why cards always showed "Unknown Course" / "Manual Entry" regardless of the real data.

## 2026-08-17

- **`6531c21`** `[Feature]` **[Bug Fix]** Admin Questions page now displays and orders questions by their original source-document sequence instead of database insertion order. Parser captures each question's literal printed number (e.g. "Q.26)" → 26) with a parse-order fallback for documents with no explicit numbering; stored in the previously-unused `question_number` column. Since that column is text (not integer), sorting is done numerically client-side to avoid `"10"` sorting before `"2"`. Also fixed the Edit Question workflow: Preview and Save now read each rich-text editor's live content via a ref instead of depending on the editor's `blur` event having already fired (was dropping the very last edit in some cases), and `questionService.update`/`.create` errors (which Supabase returns as `{error}`, never throws) are now surfaced instead of silently ignored — a second, independent cause of "my edit didn't save."
- **`ae453b3`** `[UI]` **[Bug Fix]** Admin Users and Demo Leads tables were unusable at real 100% browser zoom — the rightmost Actions column required horizontal scrolling to reach. Trimmed padding/min-widths and made the action column `sticky right-0` so it's always reachable. Added dynamic summary cards: Users page shows live Student/Tutor/Parent/Admin totals; Demo Leads shows Total Students / by Test / by Grade / by Status, reactive to the active filters (added a Grade filter that didn't exist before).
- **`cdf5fb7`** `[Bug Fix]` DOCX content fidelity, three independent root causes found and fixed:
  1. A "broken LaTeX recovery" heuristic in `MathRenderer` was rewriting plain English words — `gamma`, `times`, `alpha`, `beta`, `theta` — into LaTeX commands whenever followed by whitespace, which MathJax then rendered as Greek letters/math symbols (`"gamma ray"` → rendered as `"γ ray"`; `"3 times"` → rendered as `"3×"`, reported as `"3X"`). Reproduced and confirmed both exact symptoms before/after the fix. Scoped the recovery to only fire when a structural command (`frac`/`sqrt`/`text`) is immediately followed by an argument bracket — never valid English.
  2. Admin Question editor accepted only one character at a time / lost cursor focus after backspace: the four Jodit rich-text editors fed their own `value` back through `onChange` on every keystroke, and `jodit-react` force-resets its internal DOM whenever the `value` prop changes — confirmed in the library's own source. Switched to `onBlur`.
  3. Bulleted/numbered lists in source DOCX files were being silently flattened into plain-text lines during parsing (no `<w:numPr>` handling at all). Parser now reads `word/numbering.xml` and rebuilds real, correctly-nested `<ul>/<ol><li>` HTML, verified end-to-end against a real production DOCX.
- **`bbc69a6`** `[UI]` **[Bug Fix]** Redesigned the Student Groups page and AI Companion UI per explicit "UI only, don't touch functionality" instructions. AI Companion: added a working back button (two-stage: mid-conversation reset vs. navigate away), a functioning microphone/speech-to-text button (fixed a transcript-duplication bug caused by appending each `onresult` callback instead of rebuilding from a snapshot), and removed the Premium-only restriction on topic practice per instruction (both plans now get full access, quiz count is a suggested/editable prefill rather than an immediate auto-send). Exam typography: wired the already-loaded Inter font into Tailwind's `font-sans` stack (it was loaded via Google Fonts but never actually applied, silently falling back to the OS system font — the real cause of "text too thin/small"). DOCX parser: widened the option-extraction regex to handle a question stem ending directly against an option letter with no space (`"...phrase?A)theory"`).

## 2026-08-16

- **`106eda8`** `[Bug Fix]` **[Improvement]** Universal leaderboard: fixed a crash from undefined variables, replaced fragile Full-Length/AP heuristics with proper `main_category`/`is_adaptive` checks, fixed group/assignment scoping, added assigned/completed/highest/average stats. Demo Leads: normalized email matching so the "latest submission" logic actually finds duplicates, refreshed `created_at` on update, added Test Type/Specific Test/Status/Search filters. Parallelized independent Supabase queries across student/tutor dashboards (measured 433ms→132ms on one dashboard). Fixed a Supabase 1000-row default-cap bug in KB topic matching and course question-counts that was silently truncating data.
- **`8b07c81`** `[Feature]` Added hierarchical group analytics and the first version of the universal shared leaderboard (student/tutor/admin/parent).

## Earlier history (pre-changelog)

*Summarized from commit messages only — no detailed root-cause context recorded before this file was created.*

- `7b8cf8f` (2026-08-14) — Fix Demo email parent dispatch to use single call
- `a0fbe9f` (2026-08-14) — Update Demo report PDF generation and email logic
- `9b3d1c0` (2026-08-14) — Fix text visibility in short answer input field
- `f036160` (2026-08-14) — UI redesigns for Practice Quiz, AI Tutor modal, sidebar feedback removal, and level dashboard updates
- `c6cbabd` (2026-08-12) — Fix nested routing unmounts in App.jsx by grouping routing keys to prevent layout jumps
- `4a1e528` (2026-08-12) — Fix AITutorAgent unmount crash by preventing useEffect from returning scrollIntoView result
- `9eb3757` (2026-08-12) — Fix test_submissions queries crashing due to non-existent status column
- `d57a4be` (2026-08-12) — Fix sidebar scroll resetting on route change
- `0480f24` (2026-08-12) — Optimize test_submissions queries to fix slow loading pages
- `8e24c80` (2026-08-11) — Fix UI zoom issues and PDF print clipping
- `da7e985` (2026-08-11) — Linear SAT student flow, file deletion fix, dynamic card status & report titles
- `e9d8645` (2026-08-11) — Enhance Weakness Drills filters, Student Course List buttons & background consistency, and Test Review real-time scores for all test types
- `6e3986b` (2026-08-10) — Redesign Level Selection and Level Dashboard UI
- `c7e6e13` (2026-08-10) — UI enhancements for file uploads and sidebars
- `ef53929` (2026-08-10) — Update Full Length Test UI and implement Linear SAT scoring tables
- `bde0749` (2026-08-10) — Complete Admin Dashboard UI upgrade and Linear SAT scoring
- `cf8c141` (2026-08-07) — Fix Admin Dashboard data integration and UI enhancements
- `e6ccffe` (2026-08-07) — Enhance Demo Landing Page, Demo Leads filters, and Demo Test Report logo
- `87d6e2e` (2026-08-06) — Implemented Jodit custom Math plugin, updated Demo Test UI for naming/branding, and improved course module views
- `c45d808` (2026-08-05) — Enhance demo leads report viewing and save submission analytics
- `b0bc874` (2026-08-01) — Handle PGRST204 error for undefined columns during group update
